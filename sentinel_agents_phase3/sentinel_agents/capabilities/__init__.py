"""Capability implementations.

Each capability must define a function whose name matches the
capability (e.g. ``search`` for the ``search`` capability).  The
executor imports and invokes these functions based on the capability
name passed by the agent runtime.
"""
